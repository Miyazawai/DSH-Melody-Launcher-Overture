# CONTEXT — DSH Launcher 领域词汇表

本文件只收录**领域术语**及其精确含义，不含实现细节。术语在对话中定下来就立即写入。

## 核心理念（C 端优先 · 小白优先）

> 本节是产品哲学与决策准绳，不描述实现。实现层面有出入时，以本节为演进方向。

### 总纲：下开即用

- 本分支（序曲，C 端）的一切设计以**小白优先**为第一原则：拿到就能用，看不懂也不慌。
- **下开即用**：C 端用户下载启动器后，自带一份官方推荐整合包作为基础环境，无需任何装配即可启动使用原生优化过的 DSH。
- 遇到插件类兼容性问题，由 agent 负责修复与取舍，不要求用户懂技术。

### 整合包 = 一个单位

- 整合包是 C 端的心智单位：一个包 = 一套 **Agent 预设 + 技能 + 插件 + 配置**，并引用一个机器级共享的 **DSH 版本**。
- **DSH 本体不进整合包**：同版本整合包共享同一份 DSH 安装，保证轻量。导入的整合包若引用本机没有的 DSH 版本，导入/启动时**弹窗确认**（提示需下载的版本与体积）后才下载——现状是静默自动下载，理念要求改成确认式（待实现）。
- 兼容性修复发生在**包内层**（锁插件版本、source 形态、构建白/黑名单、包内补丁、应用独立运行），**不需要修改 DSH 本体**。
- 以下内容**不属于**整合包内容，理念上明确区分：
  - **应用加载项**：机器级全局资源（独立运行目录），整合包只记录"依赖哪些应用 + 随包启停联动"，本体跨包共享（与 DSH 版本同理）。
  - **任务工作区内的临时依赖**（例如 agent 为做视频在工作区 `npm install remotion`）：装在工作区，随任务存亡；启动器的市场/整合包体系不管理它，也不进入导出包。

### 官方默认整合包机制

- 官方默认整合包以 **GitHub Release 资产**（`official-pack-v<版本>.zip`，发布在 Overture 仓库 `LAUNCHER_RELEASE_REPOSITORY`）分发。**版本号 = 适配的 DSH 版本 + 序号**（如 `0.1.5-rc.2.1`），用户看版本号就知道这个包适配哪个 DSH；`OFFICIAL_PACK_VERSION` 常量退化为「列表读不到时的内置兜底」，实际推荐版本以 Release 列表里最新的那个为准。
- **界面上「适配 DSH X」只有两个可信来源**：Release 正文的 `dsh:` 行，或版本号命名里的编码（去掉尾段序号后仍是合法版本号，如 `0.1.5-rc.2.1` → `0.1.5-rc.2`）。两处都读不出（旧命名的历史资产 `official-pack-v0.1.1.zip` 即此类：包版本 = 启动器版本，挂的 Release 正文是更新日志）时 `dshVersion` 为 `null`，界面先用**本机已装这个包的真实绑定版本**补上，仍补不上就显示「适配 DSH 未标注」——绝不拿包版本号冒充（早期实现这么干过，于是真机出现「适配 DSH 0.1.1」这种假信息，实际适配 0.1.2-rc.1）。
- **一次可挂多个版本**：整合包页有一个「官方整合包」折叠堆叠入口，展开是各版本的官方包（最新版标「推荐」，其余可下历史版本），每一项点「下载」即导入成普通整合包（带「官方」徽标）并可直接使用。重复下载同一版本会得到新包「官方默认整合包 <版本> (2)」——同名导入的序号后缀由 `resolveImportedProfileId` 给出，显示名必须由「加后缀前后的两个 id」比对推导（不能拿中文显示名反推 id，中文经 `packProfileName` 会变成一串 `-`，官方包重复下载正是这个坑）。
- **首装自动、新版只提醒**：启动核对分两段——本机**一个官方包都没有**（全新用户）时自动导入推荐版本，保证开箱可用；**已有官方包但存在更新版本**时只把状态推给渲染层、在堆叠入口挂「有新版本」徽标，由用户自己决定何时下载，不静默拉几百 MB。状态由主进程缓存（启动核对与渲染层首次读取会**合流成一次 GitHub 查询**）。
- **可删可恢复**：官方包不是枷锁，正常删除；删掉后在堆叠里一眼能看到推荐版本未下载，一点即可重取。包记录带 `officialVersion` 戳，列表挂「官方」徽标。版本列表读不到（断网/限流）且本机还没有官方包时，标题栏给一个「恢复官方整合包」兜底按钮（盲装推荐版本），避免无路可走。
- **新包并存，绝不改动已有的包**：新版官方包以**新包导入**方式落地，与旧官方包并存，旧包降级为普通包（可删）。绝不覆盖或合并进用户已有的任何包；用户基于官方包做过的改动不受影响。也因此不存在「就地升级包内插件」的路径。
- **导入后外观回原生**：官方包内容由发布方决定，导入成功后统一把外观钉回 DSH 原生无皮肤无壁纸（`electron/stock-appearance.ts` 写 `skin-center-active.json` = `{ active: null, initialized: true }`）。`active: null` 是 skin-center 自己文档里的 "stock look" 标记，`initialized: true` 一起堵住 `seedDefaultActiveSkin` 的强写作者皮肤分支；**只在导入时写一次**，用户之后自己挑的皮肤/壁纸不会被反复覆盖。
- **大包下载自动换源**（`electron/official-pack-download.ts`）：官方包 100MB+，国内直连 GitHub 常是「连得上但只有几十 KB/s」，所以候选链（用户配置的镜像 → GitHub 直连 → 内置公共镜像）配三重守卫：8 秒等不到首字节、中途断流 20 秒、5 秒观测窗口内均速低于 300KB/s，任一命中就换源；若所有源都慢，第二轮按实测速度从快到慢重跑一次且不再因慢中断（否则慢速线路会一直换源、永远下不完）。进度以 `packProgress` 的 `download` 事件上报（字节数 / 总量 / 滑动平均速度 / 当前源，主进程 250ms 节流），整合包页据此画进度条并回显「经 gh-proxy.com」这类来源；首页仍吃同一份文字版。
- **下载完成 ≠ 装好**：之后还要写盘、补装缺失的 DSH 运行时（约 100MB，走 runtime-versions 的安装流水线）、解压一万多个文件，这几段以前没有进度、进度条会停在 100% 像卡死。现在用 `packProgress` 的 `stage` 事件串起来：主进程在「下载完成」与「补装运行时」两处显式发阶段事件（补装期间把 `installProgress` 接力到整合包页，见 `relayDshProgressToPack`），解压沿用既有的 `extract`（按文件数）。整合包页的同一条进度条依次显示：字节进度 → 「正在准备 DSH x.y.z · 64%」→「正在解压快照包… · 97%」→ 结束，没有百分比的阶段退回不确定态动画。
- 官方包内容基线：web-all 全家桶（`@linxin666/dsh-web-all`）+ officecli 技能四件（含包自带 `tools/officecli/officecli.exe`）+ humanizer-zh + find-skills，外观为 DSH 原生无皮肤。基线资产（技能文本、settings、皮肤状态、`pnpm-workspace.yaml`）在仓库 `official-pack/` 目录里受版本控制，构建不再依赖维护者本机。
- **出包由 CI 自动完成**（`.github/workflows/official-pack.yml`，每日 + 手动触发）：判据**只看 `@linxin666/dsh-web-all` 是否发新版**（officecli 自己发版不触发出包，避免频繁刷包），一旦出包就把 officecli 顺带升到当时的最新 tag（可用 `workflow_dispatch` 的 `officecli-tag` 钉回旧引擎）。构建走 `npm run pack:official`（`scripts/build-official-pack.mts`），与整合包页「导出」同一条快照管线（个人数据剔除规则自动生效），产出 zip 与一个 `official-pack-meta-<版本>.json`（记录本次用的 web-all / officecli / dsh，供下次判据与事后追溯）一起挂到 Overture 仓库的**最新** Release。两个硬约束：资产名必须严格等于 `official-pack-v<版本>.zip`（多传的 meta json 启动器完全无视，它只认资产名），列版本走 `releases?per_page=100`（套镜像候选链、按版本降序，第一项即推荐版本），**只有**列表整体读不到时才退回 `releases/latest` 找同名资产。跨仓库上传需要 secret `OVERTURE_RELEASE_TOKEN`（fine-grained PAT）；没配时只产出 artifact，不自动上传。

### 失败引导（PCL2 式）

- 启动失败时弹窗给出：**错误摘要 + 完整日志（可一键复制/导出）+ 一键复制的修复引导提示词**，供用户交给其他 agent 或原版 DSH 修复。
- 修复引导提示词要求包含足够上下文（包、DSH 版本、失败命令、退出码、关键日志片段），让外部 AI 无需追问即可开始。
- 本分支承诺 v1 =「给提示词与日志」；**启动器内一键代修**为远期愿景，不承诺版本。

### 远期愿景（不承诺版本）

- 整合包市场（分发官方与社区整合包）。
- 启动器内直接调用 agent 一键代修兼容性问题。

## 核心对象

- **DSH（DeepSeek Harness）**：本启动器托管的本地 AI 工作台运行时，本体通过可执行命令启动一个 Web 工作台。
- **启动器（Launcher）**：管理 DSH 的桌面程序本体（即本项目）。同一代码库内同时服务两类用户：C 端小白与开发者。

## 环境与隔离

- **DSH 版本**：本机可并行安装的多个 DSH 运行时版本，机器级共享；整合包各自引用其中一个版本启动，多个整合包可以引用同一个版本。「DSH版本」页面只负责版本的下载安装与管理，**环境切换只发生在整合包页**；每安装一个 DSH 版本会自动诞生一个以它命名的整合包。
- **发布渠道与推荐版本**：DSH 只从 npm 分发，版本号里带预发布后缀即发布渠道（`alpha` 内测 / `beta` 公测 / `rc` 候选发布 / 无后缀为正式版）。渠道一律从版本号解析，**不采信 npm dist-tag**——它是发布者自起的别名，常与版本号对不上（实测上游 `next` 标签指向的是 `rc` 版本）。首次安装与一键更新按「**稳定优先、同级取新**」选版本（正式版 > rc > beta > alpha > 其它，同渠道取版本号最高者），规则单点实现在 `electron/dsh-release.ts`，与「检查更新」共用同一口径，避免「提示可更新到 X、装下去却是 Y」。版本页按渠道归堆展示：版本号最高者标「最新版」，实际会装的那个标「推荐安装」。上游至今未发正式版，npm 的 `latest` 标签因此长期停在旧的预发布版上，不作为选版依据。
- **一级导航（顶栏 tab）**：常驻顶栏把管理能力拍平成同级页面：启动、整合包、DSH版本、插件、技能、预设（顺序单点定义在 `src/lib/nav.ts`）。同一固定尺寸窗口内换页；「开发人员选项」管理界面已随「暴力整合包模式」路线整体移除，界面只有这一层。
- **启动页（启动 tab）**：一级导航的默认页——左品牌列（logo / DSH-Melody-Launcher / 版本 / QQ群 / 设置入口），右侧小部件轮播区，右下方启动大按钮（状态写进按钮文案）、运行时顶栏出现「打开网页」、「切换整合包」跳转。

## 可独立管理的资源（各有各的目录）

- **插件（Plugin）**：以 DSH 加载层形式生效的组件，随 Profile 装入；开关决定是否在启用清单里。
- **技能（Skill）**：整合包内的资源，不跨整合包共享；安装时归属于当时激活的整合包，可整体开关。
- **Agent 预设（Preset）**：整合包内的资源，不跨整合包共享；安装时归属于当时激活的整合包，可整体开关。
- **应用加载项（Application Addon）**：可作为 DSH 替代 Web 托管方的独立应用，独立目录，可整体开关。
- **OfficeCLI 工具（外部二进制）**：office 三件套技能的操作引擎（约 33MB 单文件）。启动含 officecli 技能的整合包前，启动器按**三级顺序**解析可用二进制：① **包自带** `<包家目录>/tools/officecli/officecli.exe`——官方预设包的主路径，二进制随快照导出/导入搬运，别人导入即有、零网络，版本随官方包发布滚动更新；② 系统 PATH 已有（用户自装过）直接复用；③ 都没有才走托管下载兜底（解析 GitHub Release → 用户 GitHub 镜像 → 直连 → 内置公共镜像候选链，单源 15 秒断流即掐、5 分钟硬顶，SHA256/大小校验后装进机器级 `userData/dsh-tools/`，24h 版本缓存）。命中路径注入 DSH 子进程 PATH；准备进度显示在首页启动按钮与整合包页横幅；3 分钟启动硬顶，任何失败不阻塞启动（退化为技能自带的联网自愈）。

## 整合包

- **整合包（Pack）**：一个真隔离的本地环境实体，持有自己的一套插件、技能、Agent 预设、配置与会话历史，并引用一个机器级共享的 DSH 版本。名字可改，同名新建时自动加「(2)」这类序号后缀。可整体导出/导入（部分含离线本体）。整合包激活期间安装的资源共享归该整合包所有。「配置文件（Profile）」不再是用户概念，只是包内的实现细节。
- **激活 / 切换**：把启动器环境指针切到某个整合包；DSH 以激活包的家目录启动。切换只在整合包页发生，运行中禁止；没有「停用」一说。正常情况下恰好有一个激活包；删光所有包后进入**零包引导态**（指针为空：整合包页与首页引导新建/导入，启动被拦，此时新建包或下载版本会自动成为当前包）。
- **新建（出生）**：两种方式——手动新建空白包（必选一个已安装的 DSH 版本）；导入整合包归档（缺少的版本自动下载）。首次迁移会内置一个 `web` 默认包；下载 DSH 版本不再自动建包。
- **删除**：连该包的整个私有环境（会话、登录、插件、技能、预设）一并删除，共享的 DSH 版本二进制不动；激活中的包也可删（需先停 DSH，删后自动落到剩余整合包，一个不剩则进入零包引导态）。
- **导出 / 导入**：整合包以**快照**形式带走——把包的家目录整体打成压缩包（插件本体、插件配置、技能、预设、外观、依赖本体全带），导出前剔除个人数据（API 凭据、会话历史、用量、任务板、附件、日志）并把绝对路径相对化；导入 = 解压成新的隔离家目录，**导入即可启动**（依赖随包，不需要联网重装）。包名取压缩包文件名（导入时可改），DSH 版本读包内 `profile.yaml`。旧格式（清单包 / yaml / 扫描包）导入仍兼容。

## 模型与指令

- **模型（Model / 提供商）**：可在配置文件中登记的多家 API 提供商与模型（如 sensenova、opencode、deepseek 等），用于 Agent 对话与工具调用。
- **权限（Permission）**：DSH 对操作的授权策略（如默认预设）。

## 界面分层（2026-09 更新）

- **单一分层**：界面只有顶栏一级导航（启动 / 整合包 / DSH版本 / 插件 / 技能 / 预设）+ 品牌区「设置」对话框（DSH 目录、端口、主题等启动器自身设置）。原「开发人员选项」管理界面与开发者模式已整体移除。
- **MCP**：尚未成型的对象——本机 DSH 当前没有 MCP 目录或配置，本阶段不做；将来 DSH 支持时再以"带开关的独立目录资源"形态加入。